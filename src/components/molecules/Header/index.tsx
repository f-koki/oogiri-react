import React from "react";

const Header: React.FC = () => {
  return (
    <header className="Header">
      {/* TODO: バーガーメニュー作成 */}
      <div>burger</div>
    </header>
  );
};

export default Header;
